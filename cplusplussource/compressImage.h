#ifndef COMPRESS_IMAGE_h
#define COMPRESS_IMAGE_h

#include <vector>
#include <string>

std::vector<uint8_t> compressImage(uint8_t* image, uint32_t sleepTime, uint16_t x_res, uint16_t y_res);

#endif
